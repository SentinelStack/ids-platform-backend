# Copy to .env (gitignored) and fill in the real password.
#   cp .env.example .env
# Then load + run:
#   set -a; source .env; set +a
#   mvn spring-boot:run
MONGODB_URI=mongodb://ids_user:YOUR_PASSWORD@194.62.122.30:27017/ids_platform?authSource=ids_platform
