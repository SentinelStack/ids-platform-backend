#!/usr/bin/env bash

set -euo pipefail

export JAVA_HOME="${JAVA_HOME:-$(/usr/libexec/java_home -v 17)}"
export PATH="$JAVA_HOME/bin:$PATH"

echo "[pre-commit] Running Checkstyle and PMD..."
mvn -q -DskipTests checkstyle:check pmd:check

echo "[pre-commit] Checks passed."
